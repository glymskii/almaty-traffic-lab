node_modules/
dist/
.vite/
coverage/
*.log
.DS_Store
.env
.env.*
!.env.example
.vercel/
playwright-report/
test-results/
# raw OSM tiles are committed compressed; never commit the uncompressed working copies
data/osm/**/*.json
!data/osm/**/*.json.gz
# copied from data/networks/ by apps/web/scripts/copy-networks.mjs (predev/prebuild); data/ is the source of truth
apps/web/public/networks/
# generated by the desktop app in spawned worktrees (copy of CLAUDE.md)
AGENTS.md
# local Claude Code harness state: spawned worktrees and machine-specific launch config
.claude/worktrees/
.claude/launch.json
