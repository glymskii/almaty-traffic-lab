{
  "name": "@atl/sim-core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Pure deterministic microscopic traffic simulation (IDM + MOBIL, signals, intersections, buses, pedestrians, metrics, bottleneck detector). No DOM, no Three.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "sim": "node src/cli.ts"
  },
  "dependencies": {
    "@atl/contracts": "workspace:*"
  }
}
