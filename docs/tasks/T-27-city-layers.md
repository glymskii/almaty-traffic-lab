# T-27. Городские слои: здания, парки, реки, время суток

**Пакет:** `packages/map-data` + `apps/web` · **Волна:** 5 · **Уровень:** C · **Размер:** ~700 строк · **Зависит от:** T-02, T-05 · **Разблокирует:** T-29

## Цель
Компилятор заполняет `buildings`, `areas`, `waterways` из снимка; рендер рисует их склеенными мешами со слоями
и сменой освещения по времени суток.

## Что сделать
1. `map-data/src/compiler/city.ts`: здания из `building=*` (way и multipolygon outer), высота `height` (м) или
   `building:levels × 3` или дефолт по типу (`apartments` 15, `house` 6, иначе 9; `default`); упрощение контуров 1 м;
   парки `leisure=park`/`landuse=grass`, вода `natural=water`, реки `waterway` с шириной по тегу или 6 м; всё в локальных метрах.
2. `apps/web/src/scene/city.ts`: здания через `ExtrudeGeometry`/`ShapeGeometry` (earcut в three) склеены в ≤ 10 мешей,
   плоское затенение, два оттенка по высоте; парки и вода плоскими полигонами чуть ниже полотна; реки лентами.
   Слой «Здания» и «Зелень/вода» с выключателями во вкладке «Обзор».
3. Время суток: цвет неба/фона и интенсивности света по `timeOfDayMin` (день/вечер/ночь), фары как эмиссивный цвет инстансов
   при темноте (T-13 добавить флаг «фары»).
4. Тесты: высота по тегам, упрощение полигонов, число мешей ≤ 10 на маленьком квадрате.

## Критерии приёмки
- [x] Маленький квадрат узнаваем (кварталы, Атакент, стадион); 60 fps с включёнными зданиями; `pnpm check` зелёный.

## Заметки от T-10 (учесть)
- Дискреционные перестроения переоцениваются раз в 5 шагов со сдвигом по индексу слота (константа
  `DISCRETIONARY_EVERY` в `packages/sim-core/src/simulation.ts`) — это главный рычаг стоимости стадии 5.

## Заметки после ревью (интегратор, слияние в main)

Слито в main `merge --no-ff` (конфликты: `apps/web/src/Viewport.tsx`, `apps/web/README.md`,
`packages/map-data/README.md` — все из-за параллельного T-24, разрешены вручную с сохранением обеих
сторон, не потеряно ни одной строки функциональности). `packages/contracts` в дифе ветки не тронут —
проверено (`git diff --stat main...origin/task/T-27 -- packages/contracts` пуст).

- **Поломка после слияния (исправлено):** `apps/web/test/ScenariosTab.test.tsx`'s `fakeViewport()` —
  написанный в T-24 до появления поля `cityLayers` в `ViewportHandle` — не собирался (`tsc`:
  `Property 'cityLayers' is missing`). Добавлено `cityLayers: {} as ViewportHandle["cityLayers"]` в
  фикстуру; тест не использует это поле, так что заглушки достаточно. `pnpm check` зелёный после правки
  (typecheck/lint/determinism/vitest: 489 passed / 19 todo).
- Код и данные проверены по факту (не только по отчёту исполнителя): читал `city.ts` целиком, сверил
  сигнатуры со схемами `packages/contracts/src/network.ts` (`Building`/`Area`/`Waterway`,
  `ProvenanceMapSchema`), перепрогнал `packages/map-data/test/compiler/city.test.ts` — 14/14, включая
  приёмочный тест на реальном снимке small: `2254 buildings, 509 areas, 12 waterways` — совпадает с
  отчётом исполнителя дословно.
- Визуально проверено через `pnpm dev` (Browser pane): маленький квадрат узнаваем (кварталы, круглое
  здание похоже на Атакент/стадион, парк и водная лента вдоль сети), переключатели «Здания»/«Зелень и
  вода» работают независимо (проверено вкл/выкл), пресет «Вечер 18:30» меняет цвет неба и освещение
  корректно (тёплый оттенок, здания темнее).
- **FPS/rtFactor в headless-браузере агента низкие** (FPS ≈ 29, коэфф. реального времени ≈ 0.01 при
  ~4000 машинах с включёнными зданиями) — это ожидаемо для сэндбоксного окружения без GPU-ускорения, не
  обязательно отражает реальное железо; сам исполнитель уже пометил этот риск в заметках к T-29/T-28.
  **Следующей задаче, трогающей рендер-перформанс (T-28), стоит явно перепроверить FPS с включённым
  слоем «Здания» на настоящем браузере/железе**, а не полагаться на headless-цифры (ни мои, ни
  исполнителя).
- В `data/networks/almaty-abay-small.assumptions.json` стадия `city` даёт 61 предупреждение
  `"...no usable geometry..."` (way/`outer`-член без геометрии в снимке — краевой эффект Overpass-запроса
  импортёра T-01 на границе bbox, задокументирован в `packages/map-data/README.md` в разделе про реки).
  Это ~2.7% от общего числа зданий/areas и не мешает критерию приёмки (2254 здания всё равно
  скомпилированы), но если T-29 (большой полигон) увидит похожую или большую долю пропусков —
  стоит завести отдельную задачу на импортёр (T-01), а не чинить это в city.ts.
