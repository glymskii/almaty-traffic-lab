# T-26. Сравнение сценариев A/B

**Пакет:** `apps/web` · **Волна:** 5 · **Уровень:** M · **Размер:** ~600 строк · **Зависит от:** T-24, T-25 · **Разблокирует:** T-31

## Цель
Два воркера с одним сидом (базовый и сценарий), переключатель того, что рисуется, панель дельт и список
«новые узкие места».

## Что сделать
1. `sim/abRunner.ts`: два `SimClient`; общий контроль времени: команда `play` обоим, синхронизация по `simTimeS`
   (если один отстаёт более чем на 5 с, второй ставится на паузу до выравнивания); прогрев обоих; смена сценария B
   пересоздаёт только B.
2. Переключатель A/B в баре времени (клавиша Tab): рендер читает кадры выбранного клиента; тепловая карта и Топ-N тоже.
3. Вкладка «Сравнение»: таблица `totals` A/B/Δ (задержки маш-ч и чел-ч, средние скорости по классам, доля E/F), Топ-N
   рядом, «Новые узкие места» (id в B, которых нет в A) и «Исчезнувшие» (наоборот), «Переехавшие» (совпадающий узел, другой подход).
4. Кнопка «Применить рекомендацию в B» из панели узких мест (T-25) создаёт сценарий B и перезапускает его.
5. Тесты: расчёт дельт и классификация новых/исчезнувших/переехавших на двух синтетических отчётах; синхронизация времени
   на фейковых клиентах.

## Критерии приёмки
- [ ] Сценарий «стрелка налево» на выбранном перекрёстке показывает уменьшение задержки в B и, если есть, новые узкие места; `pnpm check` зелёный.

## Заметки из ревью T-23 (учесть)
- Вкладка «Сравнение» сейчас заглушка в `apps/web/src/ui/App.tsx` (см. `STUB_TASK_ID`) — замени её содержимое;
  переключение вкладок в сторе (`apps/web/src/state/store.ts`) уже рабочее и трогать его не нужно.

## Заметки от T-19 (интеграция детектора в main, учесть)
- **«Новые узкие места» ищутся по `item.id`** = `${linkId}:${nodeId ?? "mid"}`. Он стабилен между двумя
  отчётами об одном окне, между окнами и между сценариями.
- **Риск «`applyOverrides` пересоздаст линки с новыми id» проверен и не подтвердился.** Замер интегратора
  на `almaty-abay-small`: `applyOverrides(net, [{kind:"link", linkId:"w1300898059_1_f",
  set:{leftPocketLengthM:90}}])` — то есть override, который реально перестраивает полосы и регенерирует
  узлы, — сохраняет все 896 id линков, все 444 id узлов и все 1675 id полос без изменений. Сравнение A/B
  по `item.id` безопасно.
- **Числа сравнивать осторожнее, чем id.** `delayVehH`/`delayPersonH` элементов оконные и пилят ±6 %
  с периодом `windowS / 8` (заметка T-18), а `vcRatio`/`los` считаются по реализованному потоку и в
  сильном заторе падают вместе с пропускной способностью (разбор — «Заметки после ревью» в карточке
  T-19). Для «стало лучше/хуже» опирайся на `totals`, `speedRatio`, `queueM` и состав `causes`.
