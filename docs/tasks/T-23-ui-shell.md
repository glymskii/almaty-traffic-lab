# T-23. Оболочка UI

**Пакет:** `apps/web` · **Волна:** 5 · **Уровень:** C · **Размер:** ~800 строк · **Зависит от:** T-13 · **Разблокирует:** T-24, T-25, T-30

## Цель
React-оболочка вокруг сцены: раскладка, управление временем, пресеты времени суток, глобальные параметры,
HUD, загрузка сети и прогрев, атрибуция, легенда допущений, все строки в `i18n/ru.ts`.

## Контекст
`docs/DECISIONS.md` D2, D12; `packages/contracts/src/sim-config.ts` (`RUNTIME_SAFE_PARAM_PATHS`, дефолты);
`packages/sim-worker` (`SimClient`); T-13 подключение клиента; `CLAUDE.md` правило 7 (i18n).

## Что сделать
1. Раскладка: канвас на весь экран; слева панель с вкладками «Обзор», «Узкие места» (T-25), «Сценарии» (T-24),
   «Сравнение» (T-26); снизу бар управления временем; справа сверху HUD. Компоненты в `src/ui/`, состояние через
   лёгкий стор (`zustand` допустим как единственная новая зависимость, иначе `useSyncExternalStore`).
2. Управление временем: play/pause, скорость 1×/2×/5×/10×, часы симуляции, кнопка «Перезапуск». Пресеты
   «Утро 08:00», «День 14:00», «Вечер 18:30» = перезапуск с `startTimeMin` и прогревом (прогресс-бар).
3. Глобальные параметры: слайдеры множителя спроса, доли навигаторов, дисциплины, нарушителей — применяются на лету
   через `setParams`; остальные (бюджет машин, интервалы автобусов, такси в выделенке) с пометкой «требует перезапуск».
4. HUD: машин активных/бюджет, `rtFactor` (жёлтый < 0.8, красный < 0.5) с подсказкой «снизьте бюджет», fps,
   средняя скорость, задержка за окно. Обновление из `metrics`/`report`.
5. Загрузка: выбор сети (`small`/`big`) из `public/networks/`, базовый сценарий, `init` → `runUntil(прогрев)` → `play`.
6. Легенда допущений: доля атрибутов `default` по категориям (считается из `provenance` сети) во вкладке «Обзор» с текстом
   «Данные условные: OSM + допущения генератора».
7. Атрибуция ODbL в углу; тултипы при наведении на перекрёсток/улицу (имя, класс, полосы, скорость и происхождение) —
   raycast по полотну, показ в CSS2D.
8. Тесты: редьюсеры стора, форматирование времени, расчёт доли допущений; smoke-рендер компонентов с `@testing-library/react`
   (добавить dev-зависимость) для панели времени.

## Файлы
`apps/web/src/ui/{App,Layout,TimeBar,Hud,ParamsPanel,OverviewTab,Tooltip}.tsx`, `src/state/store.ts`, `src/i18n/ru.ts`, тесты.

## Критерии приёмки
- [x] Все пункты работают на маленьком квадрате; ни одной русской строки вне `i18n/ru.ts`; `pnpm check` зелёный.

## Заметки от T-02 (учесть)
- Ключи видов допущений перечислены в `ASSUMPTION_KINDS` (`packages/map-data/src/compiler/assumptions.ts`); для легенды нужны строки в `i18n/ru.ts`.

## Заметки из ревью T-05 (учесть)
- `Viewport` после правок T-05 отдаёт `onReady({ engine, rig, network })`: через него панели фокусируют камеру, переключают слои (коннекторы, здания) и подписываются на кадры. Не лезть в сцену напрямую из React.
- Кнопка «Обзор» использует `rig.overview()` с плавным перелётом.
- Файлы сетей называются по id пресета (`almaty-abay-small`, `almaty-center-big`), а не по ключу (`small`/`big`): переключатель сети должен брать `BBOXES[key].id` из `@atl/map-data` (добавить зависимость) или дублировать таблицу id в web.

## Заметки из ревью T-13 (учесть)
- `ViewportHandle` (см. `apps/web/src/Viewport.tsx`) сейчас отдаёт только `{ engine, rig, network }`, без `sim` —
  `SimHandle` из `apps/web/src/sim/client.ts` уже даёт готовый `play(speedFactor)`/`pause()`/`runUntil`/`onProgress`/
  `onError` протокол (используется самим Viewport для прогрева), панели времени/HUD могут использовать его
  напрямую вместо изобретения нового протокола. Но `sim` — локальная переменная внутри `mountScene`, не прокинута
  наружу: перед тем как писать TimeBar/Hud, стоит расширить `ViewportHandle`/колбэк `onReady`, отдав туда `SimHandle`
  (или геттер на него, т.к. `sim` появляется асинхронно после прогрева, а не сразу при монтировании).
- Дефолтная камера (`CameraRig.overview`, T-05) кадрирует всю сеть целиком: при реальном масштабе OSM-квартала
  машины (~4 м) и головки светофоров получаются существенно меньше пикселя. Если HUD/тултипы T-23 будут наводиться
  на конкретную машину/перекрёсток, понадобится дефолтный зум ближе к трафику, а не top-down обзор всей сети —
  либо отдельный «приблизить к траффику» пресет камеры в дополнение к «Обзору».

## Заметки после ревью (интегратор, слияние в main)
- Слито `task/T-23` в `main` без конфликтов; `packages/contracts` в дифе не тронут (проверено `git diff --stat` — пусто);
  `pnpm check` зелёный (typecheck/lint/determinism/test, 376 тестов) на первом прогоне после слияния.
- Найден и исправлен реальный баг в `state/store.ts`: `setNetworkKey` (переключатель сети «Обзора») перезапускал
  Viewport через `restartToken`, но, в отличие от `applyTimePreset`/`restart()`, не переносил `draftRestartParams`
  в `appliedRestartParams`. Сценарий сбоя: пользователь меняет «Бюджет машин» в черновике (не нажимая «Применить и
  перезапустить»), затем переключает сеть — новый Viewport монтировался со старым бюджетом, а бейдж «Применить и
  перезапустить» оставался висеть как ни в чём не бывало, хотя перезапуск уже случился. Исправлено (та же семантика,
  что у остальных двух путей перезапуска); добавлен регрессионный тест в `test/store.test.ts` и проверено вручную
  через dev-сервер (5000 → смена сети на «Центр Алматы» → бюджет в HUD и ParamsPanel = 5000, бейдж исчез).
- Решение по пункту «требуется решение» из отчёта исполнителя (интервалы автобусов как restart-required параметр
  из п.3 карточки): контракт `packages/contracts` в этой задаче НЕ трогаем. Соглашаюсь с исполнителем — глобальный
  `demand.busHeadwayMultiplier` в панели T-23 не заводим. Per-route `headwayPeakS/headwayOffpeakS` откладываем в
  форму «маршрут» редактора сценариев T-24 (см. заметку там). Если после T-24 всё ещё будет нужен глобальный
  множитель по всем маршрутам сразу — обсуждать отдельно как расширение контракта, не явочным порядком.
- Остальное из отчёта исполнителя проверено по коду, а не только по описанию: `ViewportHandle.getSim`/`onSimReady`,
  расширенный `SimHandle` (`setParams`/`requestReport`/`onMetrics`/`onReport`/`latestFrameMeta`), легенда допущений
  из `provenance` сети, тултип-рейкаст на плоскость, HUD-пороги `rtFactor` (0.8/0.5) — всё соответствует карточке.
