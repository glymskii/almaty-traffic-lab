# План разработки Almaty Traffic Lab

Дата: 7 сентября 2026. Владелец плана: сильная модель (Claude Fable 5.1) в этом окне. Исполнители задач:
агенты в отдельных окнах по карточкам `docs/tasks/T-NN-*.md`. Решения, на которых план стоит: `DECISIONS.md`.

## Цель и критерий успеха
Показать, что с помощью ИИ можно собрать полноценную 3D-симуляцию движения города с учётом нюансов,
на условных данных, с заменяемым источником. Критерий успеха исполняемый: все тесты нюансов N01–N26
зелёные (`NUANCES.md`), N27 проходит бенчмарк, и демо на большом полигоне центра Алматы показывает
Топ-10 узких мест с объяснением причин и сравнением A/B.

## Объём (что входит)
1. Импорт OSM плитками и компилятор в `network.json` с генератором допущений и происхождением атрибутов.
2. Микросимуляция IDM + MOBIL: полосы, карманы, четыре типа левого поворота, доп. секции, выделенки,
   остановки, пешеходы, gridlock, слияния, навигаторные перестроения, разнородные водители.
3. Метрики по сегментам, детектор узких мест, объяснение через связывающие ограничения, рекомендации.
4. 3D-рендер: процедурное полотно с разметкой, светофоры с секциями, инстансы машин, здания и парки из OSM.
5. UI: панели, тепловая карта, Топ-N, подлёт камеры, редактор сценариев формами, A/B на двух воркерах.
6. Headless CLI, регрессия, бенчмарк, smoke-тест, деплой.

Вне объёма: рисование новой геометрии, адаптивные светофоры, BRT по центру, приоритет ОТ, рельеф, тени,
LLM-объяснения, бэкенд.

## Фаза 0 (сделано в этой сессии)
Скелет монорепо; `packages/contracts` со схемами сети, конфига, сценариев, причин, метрик, буферов и протокола;
`checkNetworkIntegrity`; `Rng`; интерфейс `Simulation`; билдер `straightRoad`; 35 заготовок тестов нюансов;
CLI-заглушки; `CLAUDE.md`; документация; 31 карточка задач. `pnpm check` зелёный.

## Волны и задачи
Внутри волны задачи независимы и выполняются параллельно. Следующая волна открывается после интеграции
и ревью предыдущей. Уровень: **S** сильная модель (тонкая логика), **M** средняя, **C** дешёвая
(хорошо специфицировано). Размер: примерная оценка строк кода с тестами.

### Волна 1. Фундамент
| ID | Задача | Пакет | Зависит от | Уровень | Размер | Факт | Статус |
|---|---|---|---|---|---|---|---|
| T-01 | Импортёр OSM плитками с повторами и зеркалами, снимок в репо | map-data | — | C | 400 | 697 | слита |
| T-02 | Компилятор, часть 1: топология, линки, полосы, скорости, происхождение | map-data | T-01 (фикстура) | S | 1200 | 3414 | слита |
| T-03 | Синтетические билдеры: crossroads, tJunction, mergeRamp, corridor | sim-core/test | — | M | 900 | 2145 | слита |
| T-04 | Ядро симуляции: загрузка сети в SoA, пул машин, IDM, спавн на прямой дороге, hash | sim-core | — | S | 1200 | 2849 | слита |
| T-05 | Рендер полотна из сети: полосы, разметка, карманы, выделенки, камера, подписи | web | — | M | 900 | 2095 | слита |
| T-06 | Протокол воркера: цикл, пинг-понг буферов, клиент, заглушка-симуляция | sim-worker | — | M | 700 | 1366 | слита |

### Волна 2. Перекрёстки, светофоры, маршруты
| ID | Задача | Пакет | Зависит от | Уровень | Размер | Факт | Статус |
|---|---|---|---|---|---|---|---|
| T-07 | Компилятор, часть 2: коннекторы, конфликты, зебры, ворота, точки притяжения, слияния | map-data | T-02 | S | 1400 | 2089 | слита |
| T-08 | Генератор планов светофоров по Веберу, типы левого поворота, пешеходные фазы, пересборка по overrides | map-data | T-03, T-07 | S | 900 | 1605 | слита |
| T-09 | Светофоры в рантайме: автомат состояний, стоп-линии, жёлтый, стрелки | sim-core | T-04, T-03 | M | 600 | 664 | слита |
| T-10 | MOBIL: перестроения, выбор полосы по манёвру, карманы и spillback, права доступа | sim-core | T-04, T-03 | S | 1000 | 1440 | слита |
| T-11 | Перекрёстки в рантайме: коннекторы, точки конфликта, gap acceptance, gridlock-дисциплина | sim-core | T-09, T-10 | S | 1100 | 1347 | слита |
| T-12 | Маршрутизация и спрос: деревья кратчайших путей, OD по воротам и притяжениям, навигаторы, прогрев | sim-core | T-04 | S | 1000 | 1716 | слита |
| T-13 | Рендер машин и светофоров: инстансы, интерполяция, стоп-сигналы, поворотники, лампы, пешеходы-точки | web | T-05, T-06 | M | 800 | 1946 | слита |

### Волна 3. Автобусы, пешеходы, слияния, метрики
| ID | Задача | Пакет | Зависит от | Уровень | Размер | Факт | Статус |
|---|---|---|---|---|---|---|---|
| T-14 | Автобусы в рантайме: расписание, остановки в полосе и в кармане, правила выделенки, нарушители | sim-core | T-10, T-11 | M | 800 | 934 | слита |
| T-15 | Пешеходы: потоки по зебрам, пешеходные группы, уступание при поворотах | sim-core | T-11 | M | 600 | 645 | слита |
| T-16 | Слияния: узлы merge, полосы разгона, уступание съезда | sim-core | T-11, T-03 | M | 400 | 192 | слита |
| T-17 | Компилятор, часть 3: маршруты и остановки ОТ из OSM-отношений, интервалы по умолчанию | map-data | T-07 | M | 700 | 649 | слита |
| T-18 | Метрики: сегменты, окна, задержки в машинах и людях, V/C, корневые причины | sim-core | T-11 | S | 900 | 1587 | слита |

### Волна 4. Детектор, CLI, закрытие нюансов
| ID | Задача | Пакет | Зависит от | Уровень | Размер | Факт | Статус |
|---|---|---|---|---|---|---|---|
| T-19 | Детектор узких мест, ранжирование, объяснение, рекомендации с готовыми overrides | sim-core | T-18 | S | 800 | 1623 | слита |
| T-20 | Headless CLI, RunSummary, регрессия по эталону, бенчмарк | sim-core | T-18, T-19 | C | 500 | 684 | слита |
| T-21 | Разнородность водителей, финализация gridlock и нарушителей, инварианты debug | sim-core | T-11, T-14 | M | 500 | — | **не начата** |
| T-22 | Закрытие всех оставшихся тестов нюансов, калибровка допусков | sim-core/test | T-14…T-21 | M | 800 | 253 | слита |

### Волна 5. Интерфейс
| ID | Задача | Пакет | Зависит от | Уровень | Размер | Факт | Статус |
|---|---|---|---|---|---|---|---|
| T-23 | Оболочка UI: раскладка, управление временем, пресеты, глобальные параметры, HUD, атрибуция, i18n | web | T-13 | C | 800 | 1878 | слита |
| T-24 | Редактор сценариев: `applyOverrides`, формы перекрёстка/участка, localStorage, экспорт/импорт | map-data, web | T-08, T-23 | M | 1100 | 2437 | слита |
| T-25 | Тепловая карта и панель узких мест: окраска сегментов, Топ-N, маркеры, подлёт, причины, спарклайн, миникарта | web | T-19, T-23 | M | 1000 | 1497 | слита |
| T-26 | Сравнение A/B: два воркера, переключатель, дельты, «новые узкие места» | web | T-24, T-25 | M | 600 | — | **не начата** |
| T-27 | Городские слои: здания, парки, реки из OSM; рендер; смена времени суток | map-data, web | T-02, T-05 | C | 700 | 1024 | слита |

### Волна 6. Масштаб и выпуск
| ID | Задача | Пакет | Зависит от | Уровень | Размер | Факт | Статус |
|---|---|---|---|---|---|---|---|
| T-28 | Производительность: профилирование, SoA-оптимизации, автоснижение бюджета, бенчмарк в CI | sim-core, sim-worker | T-22 | S | 600 | — | **не начата** |
| T-29 | Большой полигон end-to-end: импорт, компиляция 407 перекрёстков, QA генератора, отчёт по узким местам | все | T-17, T-27, T-28 | S | 600 | — | **не начата** |
| T-30 | CI (GitHub Actions), smoke-тест Playwright, конфигурация Vercel | root | T-23 | C | 300 | 140 | слита |
| T-31 | Документация и передача: руководство для урбаниста, «как заменить город», легенда допущений | docs | T-29 | C | 300 | — | **не начата** |

Итого 31 задача, ~24 тысячи строк по оценке. Критический путь: T-04 → T-10 → T-11 → T-18 → T-19 → T-25 → T-26.

Колонка **«Факт»** — добавленные строки `.ts`/`.tsx` в коммитах, у которых `T-NN` стоит в теме
коммита (`git log --no-merges --format='%H %s' main | grep -E 'T-NN\b'`), включая постревью-фиксы;
`—` = задача не начата. Это верхняя оценка (правки одного файла разными задачами считаются
каждой из них), но сумма — ~36,9 тыс. строк — сходится с фактическим объёмом репозитория:
**38 603 строки** `.ts`/`.tsx` в `packages/` и `apps/` на 9 сентября 2026, при плановой оценке
~24 тыс. Множитель к плановым цифрам по итогам всех волн ≈ **1.6** (по волне 1 он был 2.5 —
дальше волны укладывались точнее). По пакетам, src + test:
`sim-core` 8019 + 8297, `map-data` 6186 + 3442, `web` 6980 + 2772, `contracts` 1435 + 154,
`sim-worker` 812 + 444.

## Вехи (что можно показать)
- **M1, после волны 1:** сеть маленького квадрата отрисована в 3D, по прямой дороге едут машины из воркера.
- **M2, после волны 2:** машины ездят по центру Алматы через светофоры, поворачивают, перестраиваются, стоят в очередях.
- **M3, после волны 4:** CLI печатает Топ-10 узких мест с причинами; все нюансы доказаны тестами.
- **M4, после волны 5:** демо для урбаниста: тепловая карта, объяснения, сценарии, A/B.
- **M5, после волны 6:** большой полигон, 20 тысяч машин, ссылка на Vercel.

## Процесс передачи задач
1. Владелец открывает волну: проверяет, что зависимости в `main`, при необходимости обновляет карточки.
2. Для каждой задачи открывается новое окно агента с промптом: «Прочитай CLAUDE.md и docs/tasks/T-NN-….md,
   выполни задачу, заверши отчётом по шаблону». Уровень модели по колонке «Уровень».
3. Агент работает в своём worktree (`../atl-T-NN`, ветка `task/T-NN`), заканчивает `pnpm check` и отчётом.
4. Владелец делает ревью диффа против контрактов и критериев карточки, сливает в `main`, обновляет `PLAN.md`
   (статус) и, если в отчёте есть «Требуется решение», принимает решение и фиксирует его в `DECISIONS.md`.
5. После слияния всей волны: интеграционный прогон (CLI на маленьком квадрате, `pnpm dev`), затем следующая волна.

## Риски и меры
| Риск | Мера |
|---|---|
| Расползание контрактов агентами | Заморозка `packages/contracts`, правило «Требуется решение», ревью между волнами |
| Симуляция «взрывается» (наложения, NaN) | Инварианты в debug (T-21), тесты N26, полунеявная интеграция, ограничения ускорений |
| Генератор планов даёт абсурд на 407 перекрёстках | Отчёт допущений компилятора, QA-задача T-29 с выборочной проверкой и правилами-предохранителями |
| Производительность ниже цели | Бюджет машин, `rtFactor` в UI, T-28 до масштабирования; SoA с первого дня |
| Overpass нестабилен | Плитки, повторы, зеркала, снимок в репо; сеть не нужна для разработки |
| Агенты низкого уровня застревают на тонкой логике | Уровни в карточках: конфликты, MOBIL, детектор только сильной модели |
| Дрейф между синтетическими фикстурами и реальной сетью | Одни и те же контракты и `checkNetworkIntegrity`; T-29 гоняет реальную сеть через CLI |

## Статус
Сверено **9 сентября 2026 при финальной интеграции** (этот коммит). Пересверен **весь** раздел, а не
отдельные строки: таблицы волн (с колонками «Факт» и «Статус»), сводка по волнам и «Что осталось
незакрытым» приведены в соответствие с `main` по четырём проверкам: `git log --oneline --grep "^merge:" main`,
`git branch -r`, `grep -l "Заметки после ревью" docs/tasks/*.md`, `grep -c "it\.todo" packages/sim-core/test/nuances/*.ts`
плюс проверка по коду там, где карточка и код могли разойтись. Предупреждение о неполной сверке,
стоявшее здесь после интеграции T-22, снято.

**`pnpm check` зелёный** без единой правки: typecheck 5 пакетов, biome 255 файлов, determinism ok,
vitest 77 файлов / **610 passed / 1 todo** (N27 — бенчмарк, вне `pnpm check` по плану, ждёт T-28).
Весь набор — 9,8 с при бюджете 90 с.

**Конвейер на маленьком квадрате прогнан целиком**: `pnpm run compile --bbox small`, затем
`pnpm sim --bbox small --minutes 10 --seed 1`. Вывод сохранён в `data/runs/` (человекочитаемый
отчёт `.txt` и `RunSummary` `.json`), разбор — в разделе «Интеграционный прогон 9 сентября 2026» ниже.
Веха **M3 достигнута**: CLI печатает Топ-10 узких мест с причинами и рекомендациями на реальной
сети, все нюансы кроме бенчмарка N27 доказаны тестами.

Таблица ниже отражает **фактические мержи в `main`**, а не выданные окна. Сверять перед выдачей задач:
`git log --oneline --grep "^merge:" main`. Волны закрываются не по порядку — часть задач волн 3 и 5 уже
слита, поэтому «волна не выполнена» не означает «ни одной её задачи нет в `main`»; проверять по задачам.

| Волна | Состояние |
|---|---|
| 0 | Готово |
| 1 | **Готово**: T-01…T-06 отревьюированы и слиты в `main`. Закрыты нюансы N01, N03, N04, N25 |
| 2 | **Готово**: T-07, T-08, T-09, T-10, T-11, T-12, T-13 слиты и отревьюированы |
| 3 | **Готово**: T-14, T-15, T-16, T-17 и T-18 слиты и отревьюированы |
| 4 | **Частично**: T-19, T-20, T-22 слиты и отревьюированы; **T-21 не начата** — ветка `task/T-21` стоит на `main`, `runtime/invariants.ts` нет |
| 5 | **Частично**: T-23, T-24, T-25, T-27 слиты и отревьюированы; **T-26 не начата** — вкладка «Сравнение» всё ещё заглушка (`STUB_TASK_ID.compare` в `apps/web/src/ui/App.tsx`) |
| 6 | **Частично**: T-30 слит и отревьюирован (CI + smoke + Vercel); **T-28, T-29, T-31 не начаты** |

### Фактические размеры волны 1
Вставки строк относительно базы волны (`2b0b210`), по веткам `task/T-NN`; в скобках оценка из таблицы волны.

| ID | Факт | Оценка | Комментарий |
|---|---|---|---|
| T-01 | 877 | 400 | плюс 55 файлов сжатых снимков OSM (не считаются как строки) |
| T-02 | 3535 | 1200 | компилятор оказался в 3 раза объёмнее оценки |
| T-03 | 2145 | 900 | четыре билдера с геометрией |
| T-04 | 2914 | 1200 | ядро SoA + пул + IDM + hash |
| T-05 | 2225 | 900 | полотно, разметка, камера, подписи |
| T-06 | 1409 | 700 | протокол, клиент, заглушка |
| **Итого** | **13 435** | **5300** | 170 файлов; в `main` сейчас 14 809 строк `.ts`/`.tsx` |

По пакетам в `main`: `sim-core` 5557, `map-data` 4198, `web` 2209, `contracts` 1589, `sim-worker` 1256 строк.
Вывод для оценок будущих волн: множитель к плановым цифрам ≈ 2.5.

> Историческая таблица: она считалась по вставкам относительно базы волны и по всем файлам. Актуальные
> фактические размеры **по всем 31 задаче** — в колонке «Факт» таблиц волн выше (методика и итоговые
> цифры по пакетам — в примечании под разделом «Волны и задачи»). По итогам всех волн множитель
> оказался ≈ 1.6, а не 2.5: так дорого обошлась только волна 1.

### Интеграционный прогон до волны 2 (историческое, маленький квадрат)
> Замер сделан **до** слияния T-07…T-13: ноль машин и пустые `connectors`/`gates` описывают
> состояние того момента, а не текущее. Оставлено как история компиляции сети.

`pnpm run compile --bbox small` за 75 мс: 444 узла (junction 209, signalized 70, gate 56, dead_end 36,
bend 73), 896 линков, 1675 полос, 112.8 км; 37 предупреждений, дефолты по скорости на 66% линков, по числу
полос на 40%, по поворотам на 92% полос. `checkNetworkIntegrity` — 0 ошибок.

`pnpm sim --network data/networks/almaty-abay-small.network.json.gz --minutes 10 --seed 1` завершается с
кодом 2: CLI не реализован (T-20). Прямой прогон ядра на этой сети (10 симуляционных минут, сид 1) даёт
**ноль машин**: в скомпилированной сети `connectors`, `signalControllers`, `crosswalks`, `gates`,
`busRoutes` — пустые массивы, поэтому `baseTripsPerHour = 0` и спавн не запускается. Узлы-ворота компилятор
классифицирует (56 штук в статистике узлов), но сущности `gates` не выпускает — это работа T-07.
Метрики на тот момент были нулевые (`writeMetrics` — заглушка до T-18; с T-18 отдаёт живое окно),
`report().items` пуст (с T-19 отдаёт ранжированные узкие места).
Для сравнения, то же ядро на синтетике: `straightRoad(3 полосы, 60 км/ч)` — 66 активных машин,
средняя скорость 52.5 км/ч; `crossroads()` — 155 машин, 40.1 км/ч. То есть ядро T-04 исправно, узкое
место конвейера — компилятор части 2.

### Интеграционный прогон 9 сентября 2026 (маленький квадрат, конвейер целиком)
Полный вывод обеих команд: `data/runs/almaty-abay-small-10min-seed1.txt`, машинный `RunSummary`:
`data/runs/almaty-abay-small-10min-seed1.json`. Воспроизводится двумя командами из корня репо
(сеть детерминирована, `trajectoryHash` ниже должен совпасть — проверено повторным прогоном):

```
pnpm run compile --bbox small
pnpm sim --bbox small --minutes 10 --seed 1 --json out.json
```

**Компиляция** (235 мс): 444 узла — junction 208, signalized 70, merge 1, gate 56, dead_end 36,
bend 73 — 896 линков, 1675 полос, 112,8 км; 96 выделенок, 70 карманов. Дефолты генератора допущений:
скорость на 66 % линков, число полос на 40 %, повороты на 92 % полос; 108 предупреждений (почти все —
`lanes=N` против `turn:lanes` с другим числом записей, разрешается в пользу `turn:lanes`).
Против исторического прогона до волны 2 (тот же раздел ниже) сеть перестала быть «пустой»:
`connectors`, `signalControllers`, `crosswalks`, `gates`, `busRoutes` заполнены, спавн запускается.

**Прогон** (10 симуляционных минут после 10 минут прогрева, сид 1, `trajectoryHash cc65b4b452cc85d3`,
377 шагов/с, 8619 машин в среднем, 31,8 с настенного времени):

| Показатель | Значение |
|---|---|
| активных машин | 13 669 |
| завершённых поездок | 2 987 |
| задержка | 8,0 маш-ч / 24,7 чел-ч |
| средняя скорость | 31,2 км/ч (авто 32,0, автобусы 13,1) |
| доля стоящих машин | **93 %** |
| сегментов в заторе (LOS E/F) | **0 %** |

**Топ-3 узких места и их причины** (полная десятка — в сохранённом выводе):
1. улица Курмангазы × улица Шагабутдинова, подход с запада — 6,7 маш-ч / 10,1 чел-ч, очередь 134 м;
   пропуск пешеходов 31 %, красный 30 %, затор ниже по потоку 20 %, ожидание перестроения 13 %.
   Рекомендация: +10 с зелёного этому подходу (до 27 с).
2. проспект Абая × улица Биокомбинатская, подход с востока — 4,4 маш-ч / 6,6 чел-ч;
   уступает главной 69 %, пропуск пешеходов 30 %. Рекомендации нет (нерегулируемый подход).
3. проспект Абая × проспект Сейфуллина, подход с востока — 4,0 маш-ч / 6,8 чел-ч;
   **переполненный поворотный карман 83 %**. Рекомендация: удлинить левый карман до 120 м.

По всей десятке доминируют четыре причины: `pedestrian_yield`, `signal_red`, `gap_left_turn` и
`pocket_spillback` — то есть детектор на реальной сети показывает ровно те механизмы, которые
по отдельности доказаны нюансами N18, N05, N10 и N09. Половина рекомендаций — «добавить 10 с
зелёного», что ожидаемо: генератор планов T-08 раздаёт равные доли по Веберу без учёта пешеходной
нагрузки на подходе.

**Найдено при прогоне (не баг задачи, но требует решения владельца).** Сводка внутренне противоречива:
`stoppedShare` 93 % и `speedRatio` 0,003 на топ-1 подходе против `congestedSegmentShare` 0 %,
`vcRatio` 0,05 и **LOS A** на том же подходе. Это не новый дефект, а уже задокументированное свойство
метрик в перенасыщении: V/C и LOS считаются по *реализованному* потоку, который в заторе падает
вместе с пропускной способностью (заметки T-18 и «Заметки после ревью» T-19), а `meanSpeedKph`
смещена вверх, потому что её считают только по завершившимся поездкам. На синтетике это никого не
смущало, на реальной сети выглядит как явная ошибка отчёта: 93 % машин стоят, а сеть отчитывается
«LOS A, заторов 0 %». Практический вывод для UI и отчётов: в заголовок надо выносить `stoppedShare`
и `speedRatio`, а LOS/V-C показывать рядом с оговоркой. Кандидаты на исправление — T-28 (профиль
и бюджет) и T-31 (руководство урбаниста); отдельной задачи под это в плане нет.

**Перекомпиляция ничего не меняет.** `pnpm run compile --bbox small` поверх уже закоммиченной сети даёт
байт-в-байт тот же результат, кроме поля `generatedAt` в `assumptions.json` (и, как следствие, кроме
нескольких байт внутри `.gz`): `sourceHash` и вся статистика совпадают. Поэтому артефакты
`data/networks/` в этом коммите **намеренно не переписаны** — чтобы не плодить пустой diff. Если
перекомпиляция когда-нибудь даст другой `sourceHash` или другую статистику, это признак реального
изменения компилятора или снимка, а не шума. `pnpm sim:regress` (эталон `small-square`) — зелёный.

**Второе наблюдение — спрос.** 13 669 активных машин при 2 987 завершённых поездках за 10 минут
означает, что квадрат 112,8 км залит существенно выше пропускной способности: машины набиваются
быстрее, чем уезжают. Для демо это выигрышно (узкие места видно сразу), для калибровки — нет.
`demand.multiplier` под реальную сеть никто не калибровал: нюанс N22 проверяет только *направление*
эффекта (0,5 против 1,5) на синтетике. Это часть объёма T-29 (QA большого полигона).

### Что осталось незакрытым
Полностью пересверено по `main` 9 сентября 2026 (предыдущая версия этого списка была снимком на дату
T-18 и устарела по пунктам 2, 4, 5). Проверялось по коду, а не по карточкам.

1. **Не начаты 5 задач из 31: T-21, T-26, T-28, T-29, T-31.** Ни у одной нет ни коммитов с
   реализацией, ни ветки с работой: `task/T-21`, `task/T-26`, `task/T-28`, `task/T-29`, `task/T-31`
   существуют только локально и указывают на коммиты `main`, в `origin` их нет. Всё остальное —
   T-01…T-20, T-22, T-23, T-24, T-25, T-27, T-30 — слито и имеет раздел «Заметки после ревью»
   в своей карточке.

2. **T-21 (волна 4) — единственная дыра в закрытых волнах, и она заметная.** N19 и N26 зелёные,
   но закрыли их T-11 и T-22, а не T-21. Из объёма карточки не сделано ничего:
   `packages/sim-core/src/runtime/invariants.ts` **не существует**; флаг `debugInvariants` объявлен
   в `packages/contracts/src/sim-config.ts:198` и, по `grep` по `packages/` и `apps/`, не читается
   рантаймом ни разу — единственная ссылка на него в коде это тест N26, который проверяет инварианты
   **своим собственным обходом списков полос**, а не движком. То есть контракт есть, а реализации за
   ним нет: включение `debugInvariants: true` в любом сценарии сегодня не делает ничего.
   Также не сделаны таблица «параметр → где используется» в README `sim-core` (её там нет) и
   пересэмплирование gridlock/нарушителей по `setParams`.

3. **T-26 (волна 5) — вкладка «Сравнение» в UI всё ещё заглушка.** `apps/web/src/ui/App.tsx:22`
   держит `STUB_TASK_ID.compare = "T-26"`; `apps/web/src/sim/` содержит только `client.ts`,
   `abRunner.ts` нет. Зависимости (T-24 сценарии, T-25 тепловая карта и Топ-N) слиты, задача
   разблокирована и упирается только в исполнителя. Это единственное, что отделяет от вехи **M4**.

4. **Нюансы: 1 `it.todo` из 27** — N27 (`packages/sim-core/test/nuances/08-runtime-invariants.test.ts:154`,
   бенчмарк 20 000 машин, вне `pnpm check` по плану, ждёт T-28). N01–N26 зелёные; актуальная колонка
   «Статус» и заметки о расширенных допусках — в `NUANCES.md`. Известный компромисс: допуск зазора в
   N26 расширен до −6 м против исходного «нет отрицательных зазоров» (разбор в
   `docs/tasks/T-22-nuance-suite-closure.md`) — фикс упирается в рассинхрон upstream-просмотра
   препятствий с detour-коннектором и не локален; отдельной задачи под него нет, ближайший кандидат — T-21.

5. **Волна 6 закрыта на четверть.** T-30 слит (CI на GitHub Actions, smoke-тест Playwright,
   конфигурация Vercel). Не начаты: T-28 (профилирование, автоснижение бюджета, бенчмарк N27 в CI),
   T-29 (QA большого полигона), T-31 (документация и передача).

6. **Большой полигон импортирован и скомпилирован, но не проверен.** `almaty-center-big.network.json.gz`
   (2879 узлов, 6597 линков) лежит в `data/networks/` и версионируется; CLI умеет `--bbox big`.
   Не сделано — ровно объём T-29: QA генератора допущений на 407 перекрёстках, `docs/QA-big.md`,
   проверка загрузки и fps в браузере, отчёт по узким местам большого полигона.

7. **Вехи.** M1, M2 достигнуты. **M3 достигнута сегодня** — CLI печатает Топ-10 с причинами и
   рекомендациями на реальной сети, нюансы доказаны (см. прогон выше). **M4** упирается ровно в
   одну задачу — T-26. **M5** — в T-28 (производительность), T-29 (большой полигон) и T-31 (передача).

8. **Побочные находки, не привязанные к незакрытым задачам** (обе — из ревью T-20, оставлены как есть):
   мёртвая заглушка `withOverrides()` в `packages/sim-core/test/fixtures/builders.ts:1990`
   (всегда бросает, нигде не вызывается — можно реализовать через `applyOverrides` или удалить);
   и цикл `sim-core ↔ map-data` на уровне `package.json` (`pnpm install` предупреждает
   `WARN cyclic workspace dependencies`, реального цикла в рантайм-импортах нет).

### Как не выдать уже сделанную задачу
Осенью 2026 окно на T-11 было выдано повторно: инструкция исполнителю утверждала, что «в `main` слита
только волна 1», хотя T-11 был слит мержем `bbfa918` вместе с постревью-фиксом `a64ad51`. Агент
корректно отказался переписывать код (повторная «реализация» откатила бы правку ревью), но окно ушло
впустую. Причиной был именно этот раздел «Статус», замороженный на состоянии после волны 1.

Перед выдачей любой задачи проверять по `main`, а не по памяти или прошлому снимку:

```
git fetch origin
git log --oneline --grep "^merge:" main     # что реально слито
git branch -r                                # какие task/T-NN уже есть
ls docs/tasks/T-NN-*.md                      # карточка: раздел «Заметки после ревью» = задача закрыта
```

Наличие в карточке раздела «Заметки после ревью (интеграция …)» означает, что задача прошла ревью и
слита. Этот раздел «Статус» обновляет интегратор в том же коммите, что и мерж задачи.

Повтор случился ещё раз: 2026-09-08 окно было выдано на T-12, слитый мержем `7d33d18` неделей раньше.
Исполнитель кода не трогал и запушил `task/T-12` в тот же коммит, что и `main`, так что мерж вышел
пустым (`Already up to date`), но окно снова ушло впустую. Список слитых задач на сегодня —
T-01…T-14, T-17, T-23, T-24, T-27 (см. `git log --grep "^merge:"`); из существующих веток не слита
только `task/T-15`.

Третий подряд повтор в тот же день: следом окно выдали на T-14, слитый мержем `8155af7` и уже
прошедший постревью `b8640e7` заранее. Исполнитель снова корректно не стал переписывать код и вместо
этого перепроверил критерии приёмки по коду. Три повтора подряд (T-11, T-12, T-14) в один день — при том
что чек-лист выше существовал уже после первого случая — означают, что раздача окон не читает этот
раздел (или карточки `docs/tasks/T-NN-*.md`) программно. Стоит автоматизировать проверку одним скриптом
перед созданием чипа задачи (например: для целевого `T-NN` — `grep -l "Заметки после ревью" docs/tasks/T-NN-*.md`
и/или `git log --oneline --grep "T-NN" main | grep "^merge:"`) вместо того, чтобы полагаться на то, что
каждый агент-исполнитель заметит уже слитую задачу вручную.

**T-15 слит (2026-09-08), мержем `9bfd05b`** — единственная из перечисленных выше веток, что оставалась
не слитой, теперь тоже в `main`. Заметка для контроля дублей: `origin/task/T-15` пушился дважды за один
день. Первый коммит (`3ce1c156520b18f26ca8e21297da8e437127a29b`, от родителя `b8640e7`) был реализацией
той же карточки от более раннего окна; повторно выданный исполнитель написал модуль с нуля независимо
(не читая тот коммит построчно) и перезаписал ветку `git push --force-with-lease` — по его собственному
решению, т.к. plain push отклонялся как non-fast-forward, а карточка требовала чистого воркта от
`origin/main`. Сравнение при интеграции подтвердило независимую реализацию (разные имена/структура:
`CrosswalkRuntime` со своим `budget`/`waiting` против `PedestrianRuntime` поверх переиспользуемого
`ArrivalQueues`), а не копипасту — риска скрытого регресса это не создало, но сам факт двойной выдачи
одного окна за день — снова пример пробела «выдача окон не проверяет уже существующую ветку/коммит»,
описанного в этом разделе для T-11/T-12/T-14. Старый коммит теперь висит без ссылок в истории — доступен
только по хэшу, ничего из него в мерж не переносилось (новая реализация покрывает то же самое и больше:
она также несёт CSR-переноску `Connector.crosswalkIds` в `RuntimeNetwork`, которой в старом коммите не
было). Дальнейших действий не требуется, если только кто-то не захочет намеренно восстановить старый
коммит для сравнения — тогда используйте хэш выше, `git branch` на него не ссылается.

**T-16 слит (2026-09-08), мержем `b701783`.** Диф чистый, без конфликтов; `pnpm check` прошёл без единой
правки со стороны интегратора (533 passed / 9 todo, N20 закрыт). Подробности проверки критериев приёмки —
в разделе «Заметки после ревью» карточки `docs/tasks/T-16-merges-runtime.md`. Единственный пробел
исполнителя: `docs/NUANCES.md` не обновлялся в коммите задачи (в отличие от T-15, которая сама закрыла
свой нюанс тем же коммитом) — строка N20 и сводный абзац доведены до актуального состояния здесь, при
интеграции. Разблокирована T-22 (следующая по цепочке, волна 4) — её карточка может опереться на паттерн
`biasGateWeights` из теста N20 (смещение `weightIn`/`weightOut` на уже собранном `Network`), если билдеры
фикстур не дадут опции асимметричного спроса напрямую.